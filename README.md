# Agent Dashboard

A lean, Raspberry Pi–friendly dashboard for your OpenClaw agent.

- Minimal Python backend (FastAPI/uvicorn)
- No heavy frontend framework (plain HTML + a bit of JS)
- Designed to be extended over time

See `PLANS/agent-dashboard.md` in the main workspace for the roadmap.
